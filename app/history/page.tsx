import AppShell from "@/components/layout/AppShell";
import { activity } from "@/mock/data";
import { PageHeader, StatusBadge } from "@/components/ui/Primitives";

export default function HistoryPage() { return <AppShell><div className="content-wrap narrow-content"><PageHeader eyebrow="AUDIT TRAIL · 18 ACTIONS" title="History" description="A transparent timeline of the auction room." action={<button className="button button-outline">Export log ↓</button>} /><div className="history-filter"><button className="filter-button active-filter">All activity</button><button className="filter-button">Sales</button><button className="filter-button">Room controls</button><span className="history-date">04 SEP 2026</span></div><div className="history-list">{activity.concat([{ id: "a5", label: "Bid updated", detail: "City Lions · 11 credits on Dev Patel", time: "35 min ago", tone: "blue" as const }, { id: "a6", label: "Auction ready", detail: "Women's Premier Auction", time: "Yesterday", tone: "lime" as const }]).map((item) => <div className="history-row" key={item.id}><span className={`activity-dot ${item.tone}`} /><div className="history-copy"><b>{item.label}</b><p>{item.detail}</p></div><StatusBadge tone={item.label.includes("sold") ? "success" : "neutral"}>{item.label.includes("sold") ? "SALE" : "ACTION"}</StatusBadge><time>{item.time}</time></div>)}</div></div></AppShell>; }
