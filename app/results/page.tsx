import AppShell from "@/components/layout/AppShell";
import { players, teams } from "@/mock/data";
import { PageHeader, SectionHeading, StatCard, StatusBadge } from "@/components/ui/Primitives";

export default function ResultsPage() { const sold = players.filter((player) => player.status === "SOLD"); return <AppShell><div className="content-wrap"><PageHeader eyebrow="ARCHIVE · MEN'S PREMIER" title="Results" description="A clean record of every selection made in the room." action={<button className="button button-outline">Download report ↓</button>} /><div className="stats-grid"><StatCard label="Total spend" value="63 cr" detail="Across 4 sales" accent="#d9ef75" /><StatCard label="Highest sale" value="21 cr" detail="Arjun Mehta · NS" accent="#f28b58" /><StatCard label="Sold" value="04" detail="12.5% of player pool" accent="#8fd7e7" /><StatCard label="Unsold" value="01" detail="Returns in Round 2" accent="#c4b8ed" /></div><div className="results-grid"><section className="panel"><SectionHeading title="Sold players" action={<StatusBadge tone="success">ROUND 1</StatusBadge>} />{sold.map((player) => { const team = player.teamId ? teams.find((item) => item.id === player.teamId) : undefined; return <div className="result-row" key={player.id}><div className="player-portrait" style={{ background: player.accent }}>{player.initials}</div><div><b>{player.name}</b><small>{player.role} · {player.category}</small></div><span className="result-team">{team?.shortName}</span><strong>{player.soldPrice} cr</strong></div> })}</section><section className="panel"><SectionHeading title="Team spending" /><div className="spending-list">{teams.map((team) => <div className="spending-row" key={team.id}><div><span className="team-logo team-logo-small" style={{ background: team.color }}>{team.shortName}</span><b>{team.name}</b></div><strong>{100 - team.purse} cr</strong><div className="progress"><span style={{ width: `${100 - team.purse}%`, background: team.color }} /></div></div>)}</div></section></div><section className="panel"><SectionHeading title="Unsold players" action={<StatusBadge tone="warning">1 PLAYER</StatusBadge>} /><div className="empty-state compact"><span>↻</span><h3>Rohan Das</h3><p>Wicketkeeper · Base 2 credits · Will return in Round 2</p></div></section></div></AppShell>; }
